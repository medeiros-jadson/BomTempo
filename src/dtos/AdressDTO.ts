export interface AdressDTO {
  city: string,
  country: string,
  district: string,
  isoCountryCode: string,
  name: string,
  postalCode: string,
  region: string,
  street: string,
  streetNumber: string,
  subregion: string,
  timezone: string,
}